import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  boxTitle: {
    backgroundColor: "#7013F2",
    alignItems: "center",
    justifyContent: "center",
    padding: 30,
    marginBottom: 20,
    borderRadius: 20,
  },

  titleText: {
    color: "#DEE2DB",
    fontSize: 24,
    fontWeight: "bold",
  },
});

export default styles;
